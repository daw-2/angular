import { ClearOnDbClickDirective } from './clear-on-db-click.directive';

describe('ClearOnDbClickDirective', () => {
  it('should create an instance', () => {
    const directive = new ClearOnDbClickDirective();
    expect(directive).toBeTruthy();
  });
});
