import { Test2Directive } from './test.directive';

describe('Test2Directive', () => {
  it('should create an instance', () => {
    const directive = new Test2Directive();
    expect(directive).toBeTruthy();
  });
});
